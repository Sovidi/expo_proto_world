import { View } from "react-native";
import ScaryMain from "@/components/scary/scaryMain";

export default function TabTwoScreen() {
  return (
    <View className={`bg-[white] dark:bg-[black]`}>
      <ScaryMain />
    </View>
  );
}
